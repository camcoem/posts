export default function UserWithId({ params }: { params: { userId: string } }) {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between bg-main p-24">
      <h1 className="text-primary">HOLA</h1>
    </main>
  );
}
